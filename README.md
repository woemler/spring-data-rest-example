# Spring Data REST with JPA Example

A quick-and-dirty example project showing QueryDSL request parameter customization for JPA repositories.

## Requirements
- Java 7+
- Maven 2+

## Build and Run
- `mvn -U clean install spring-boot:run`
